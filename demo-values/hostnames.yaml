elementAdmin:
  ingress:
    host: admin.test.localhost
elementWeb:
  ingress:
    host: chat.test.localhost
matrixAuthenticationService:
  ingress:
    host: auth.test.localhost
matrixRTC:
  ingress:
    host: mrtc.test.localhost
serverName: test.localhost
synapse:
  ingress:
    host: matrix.test.localhost
